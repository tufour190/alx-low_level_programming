#include <stdio.h>

/**
 * main - program prints single digit numbers of base 10 starting from 0
 * Return: Always
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");

	return (0);
}
